  name: Azure-Containers-Sample Workflow
  on: [push]
  jobs:
    Build-Docker-Image:
      runs-on: ubuntu-latest
      steps:
        - name: Log in to Azure
          uses: azure/login@v1
          with:
            creds: ${{ secrets.AZURE_CREDENTIALS }}
        - name: Build and deploy Container App
          uses: azure/container-apps-deploy-action@v1
          with:
            appSourcePath: ${{ github.workspace }}/AzureContainersSample
            acrName: azurecontainerssample
            containerAppName: azure-containers-sample-app 
            resourceGroup: registry-tutorial

